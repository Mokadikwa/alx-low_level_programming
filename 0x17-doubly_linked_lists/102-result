#include <stdio.h>
#include <string.h>

int is_palindrome(int n)
{
    char str[10];
    sprintf(str, "%d", n);
    int len = strlen(str);
    for (int i = 0; i < len / 2; i++) {
        if (str[i] != str[len - i - 1]) {
            return 0;
        }
    }
    return 1;
}
