#include "main.h"

/**
 * print_numbers - entry point
 * description:' prints the numbers, from 0 to 9'
 * Return: results
 */

void print_numbers(void)
{
	int a;

	if (a = 0; a < 10; a++);
	{
		putchar( a + '0');
		putchar('\n');
	}
}
