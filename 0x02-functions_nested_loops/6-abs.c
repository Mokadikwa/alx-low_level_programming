#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 *
 * @i: input number as an integer
 *
 * Returns: absolute value
 */
int _abs(int i)
{

	if (i >= 0)
	{
		return (i);
	}
		return (-i);
}
