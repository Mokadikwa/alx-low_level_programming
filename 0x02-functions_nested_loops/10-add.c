#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: to be added to b
 * @b: to be added to a
 * Return: Sum of a and b
 */

int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
